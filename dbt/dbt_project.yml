name: 'pharma_vigilance'
version: '1.0.0'
config-version: 2

profile: 'pharma_vigilance'

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
seed-paths: ["data"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  pharma_vigilance:
    materialized: view
    bronze:
      materialized: view
    silver:
      materialized: table
    gold:
      materialized: table

seeds:
  quote_columns: true